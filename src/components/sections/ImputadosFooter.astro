---
import ButtonOutline from '../ui/ButtonOutline.astro';

interface Link {
  text: string;
  href: string;
}

interface Props {
  disclaimer: string;
  links: Link[];
}

const { disclaimer, links } = Astro.props;
---

<section
  class="bg-trama-red px-14 py-14 w-full flex flex-col items-center gap-6"
  data-imputados-footer
>
  <!-- Disclaimer -->
  <p
    class="text-trama-red-light text-sm font-medium text-center"
    data-imputados-footer-item
  >
    {disclaimer}
  </p>

  <!-- Links -->
  <div class="flex items-center gap-6" data-imputados-footer-item>
    {links.map((link) => (
      <ButtonOutline href={link.href} variant="white">
        {link.text}
      </ButtonOutline>
    ))}
  </div>
</section>
