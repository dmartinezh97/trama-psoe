---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class:list={['inline-flex items-center px-3 py-1.5 bg-trama-red text-white text-[11px] font-semibold rounded uppercase tracking-tag', className]}>
  <slot />
</span>
