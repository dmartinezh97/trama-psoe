---
interface Props {
  number: string;
  unit: string;
  label: string;
  variant?: 'red' | 'black' | 'dark';
}

const { number, unit, label, variant = 'red' } = Astro.props;

const variantStyles = {
  red: {
    bg: 'bg-trama-red',
    border: 'border-trama-red-dark',
    numberColor: 'text-white',
    unitColor: 'text-white',
    labelColor: 'text-trama-red-light',
  },
  black: {
    bg: 'bg-black',
    border: 'border-transparent',
    numberColor: 'text-white',
    unitColor: 'text-white',
    labelColor: 'text-trama-500',
  },
  dark: {
    bg: 'bg-trama-900',
    border: 'border-transparent',
    numberColor: 'text-trama-red',
    unitColor: 'text-white',
    labelColor: 'text-trama-500',
  },
};

const styles = variantStyles[variant];
---

<div class={`${styles.bg} h-full flex flex-col justify-end p-6 md:p-10 ${variant === 'red' ? 'md:border-r border-trama-red-dark' : ''}`}>
  <span data-stat-number class={`font-secondary text-[80px] md:text-[180px] font-black tracking-[-4px] md:tracking-[-8px] leading-none ${styles.numberColor}`}>
    {number}
  </span>
  <span data-stat-label class={`font-secondary text-lg md:text-2xl font-bold tracking-[1px] md:tracking-[2px] ${styles.unitColor}`}>
    {unit}
  </span>
  <p data-stat-label class={`font-secondary text-[11px] md:text-[13px] font-medium tracking-[1px] leading-relaxed mt-2 whitespace-pre-line ${styles.labelColor}`}>
    {label}
  </p>
</div>
