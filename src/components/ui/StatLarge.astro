---
interface Props {
  number?: string;
  unit?: string;
  label?: string;
  class?: string;
}

const {
  number = "24",
  unit = "AÑOS",
  label = "PENA SOLICITADA",
  class: className = ""
} = Astro.props;
---

<div
  class:list={[
    "w-[300px] bg-trama-red p-8 pt-10 flex flex-col justify-end",
    className
  ]}
>
  <span class="text-white text-[180px] font-black tracking-[-8px] leading-none">{number}</span>
  <span class="text-white font-bold text-[24px] tracking-[2px]">{unit}</span>
  <span class="text-trama-red-light font-medium text-[13px] tracking-[1px] leading-[1.4]">{label}</span>
</div>
